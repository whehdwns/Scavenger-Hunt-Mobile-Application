<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.login">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.ViewSubmission"></activity>
        <activity android:name=".main.WriteDescription" />
        <activity android:name=".main.TakePicture" />
        <activity android:name=".main.GradeSubmission" />
        <activity
            android:name=".main.Pop"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".main.CreateTask" />
        <activity
            android:name=".main.Instructor"
            android:label="@string/title_activity_instructor" />
        <activity
            android:name=".main.FindRoom"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".main.CreateRoom" />
        <activity android:name=".main.Student" />
        <activity android:name=".main.Register" />
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>